# Copy to `frontend/.env` (gitignored) for local development.
# On Vercel, set the same variable in Project → Settings → Environment Variables
# (Production / Preview) — no trailing slash.
#
# Spring Boot API origin — no trailing slash, no /api suffix (paths already start with /api/...).
VITE_API_BASE_URL=http://localhost:8080
